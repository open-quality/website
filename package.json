{
  "name": "openquality-cloud",
  "private": true,
  "version": "0.1.0",
  "scripts": {
    "dev": "eleventy --serve --watch",
    "build": "eleventy",
    "preview": "eleventy --serve --watch",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "three": "^0.181.0"
  },
  "devDependencies": {
    "@11ty/eleventy": "^3.0.0",
    "markdown-it": "^14.1.0"
  }
}
